a = input("ВВЕДИТЕ СУММУ ПОКУПКИ:")

if a.replace(".", "", 1).isdecimal():
    skSum = round(float(a)*0.65, 2)
    if float(a) > 20:
        print("ИТОГ:", skSum, "БУБЛЕЙ. СКИДКА:", round(float(a)-skSum, 2))
    else:
        print("ИТОГ:", a, "БУБЛЕЙ. СКИДКА: 0")
else:
    print("ВВЕДИТЕ СООТВЕТСТВУЮЩЕЕ ЧИСЛО")